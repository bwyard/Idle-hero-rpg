/**
 * processBuildings — System 5 of 12 in the tick pipe.
 *
 * Processes building production, upgrades in progress,
 * and passive income contributions.
 *
 * Pure function — no mutations, no side effects.
 */

import type { GameState } from '@idle-hero-rpg/shared';

export function processBuildings(state: GameState): GameState {
  // TODO: Implement building passive income tick (see balance.ts stubs)
  // TODO: Implement building upgrade progress
  // TODO: Implement building unlock conditions by region
  return state;
}
